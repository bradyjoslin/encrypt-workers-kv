{
  "name": "encrypted-workers-kv",
  "version": "0.0.1",
  "description": "Wrapper on Workers KV to store encrypted values",
  "main": "./encrypted-workers-kv/index.ts",
  "types": "./encrypted-workers-kv/index.d.ts",
  "scripts": {
    "build:clean": "rimraf ./encrypted-workers-kv",
    "build": "npm run build:clean && tsc --declaration",
    "test:deploy": "npm run build && cd test-worker/ && wrangler publish && cd ../",
    "test": "ava test/test.js"
  },
  "author": "Brady Joslin [brady@bradyjoslin.com]",
  "license": "MIT",
  "devDependencies": {
    "@cloudflare/workers-types": "^1.0.9",
    "ava": "^3.10.1",
    "node-fetch": "^2.6.0",
    "prettier": "^2.0.5",
    "rimraf": "^3.0.2",
    "typescript": "^3.9.6"
  }
}
