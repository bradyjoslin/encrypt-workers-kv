{
  "private": true,
  "name": "encrypted-workers-kv",
  "version": "1.0.0",
  "description": "A template for storing encrypted data in Workers KV",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "format": "prettier --write '**/*.{js,css,json,md}'"
  },
  "author": "Joslin, Brady W (Brady) <brady@bradyjoslin.com>",
  "license": "MIT",
  "devDependencies": {
    "prettier": "^1.18.2"
  }
}
